function parimpar(n) { //usando uma função com um parâmetro//
    if(n%2 == 0) { //se o número divido por dois resta 0//
        return 'Par'
    } else{ // se falso//
        return 'ímpar'
    }
}

console.log(parimpar(40)) //mostrar em tela//